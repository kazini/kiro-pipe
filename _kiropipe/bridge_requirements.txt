# Kiro API Bridge Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# LLM Integration (Universal adapter for 100+ providers)
litellm>=1.0.0

# Optional: Direct API clients (if not using LiteLLM)
anthropic>=0.7.0
openai>=1.3.0

# AWS Event Stream encoding
# (using manual implementation, no external deps needed)

# Development
pytest>=7.4.0
httpx>=0.25.0  # For testing
